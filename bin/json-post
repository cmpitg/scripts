#!/usr/bin/env rc

#
# Makes a JSON POST with Curl
#

if (! found-executable-p curl) {
	echo curl not found >[1=2]
	echo Make sure you have Curl installed >[1=2]
	exit 1
}

curl -i -H 'Content-Type: application/json' -X POST $*
