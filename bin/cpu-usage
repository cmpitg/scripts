#!/bin/bash

#
# Using `mpstat', calculates average CPU usage in 3 seconds.
#

if ! found-executable-p mpstat; then
	echo mpstat not found >&2
    echo Make sure you have Sysstat installed >&2
	exit 1
fi

mpstat 3 1 | tail -1 | gawk '$12 ~ /[0-9.]+/ { print 100 - $12"%" }'
